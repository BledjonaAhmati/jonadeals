{
  "name": "jonadeals",
  "version": "1.0.0",
  "description": "Platformë inteligjente për tregti online",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd frontend && npm install && npm run build",
    "backend": "cd backend && npm install && npm start",
    "frontend": "cd frontend && npm install && npm start",
    "test": "jest",
    "lint": "eslint .",
    "db:init": "sequelize db:migrate",
    "docker:build": "docker-compose up -d --build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "mongoose": "^7.0.0",
    "pg": "^8.10.0",
    "sequelize": "^6.29.3",
    "jsonwebtoken": "^9.0.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "eslint": "^8.39.0",
    "jest": "^29.6.1",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Web8kameleon",
  "license": "MIT"
}
